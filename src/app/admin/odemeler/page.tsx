"use client";
import { useCallback,useEffect,useState } from "react";
import { AlertCircle,CreditCard,RefreshCw } from "lucide-react";
import { AdminWaveStatus } from "@/components/admin/AdminWaveStatus";
import { listAdminPayments,reviewManualBankTransfer,type AdminPaymentRow } from "@/lib/admin/payments";
const labels:Record<string,string>={pending:"Bekliyor",requires_action:"Doğrulama gerekli",processing:"İşleniyor",paid:"Ödendi",failed:"Başarısız",cancelled:"İptal",refunded:"İade"};
export default function AdminPaymentsPage(){const[rows,setRows]=useState<AdminPaymentRow[]>([]);const[loading,setLoading]=useState(true);const[reviewing,setReviewing]=useState("");const[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);setError("");const r=await listAdminPayments();setRows(r.data);setError(r.error||"");setLoading(false);},[]);useEffect(()=>{let active=true;listAdminPayments().then((r)=>{if(!active)return;setRows(r.data);setError(r.error||"");setLoading(false);});return()=>{active=false;};},[]);async function review(row:AdminPaymentRow,decision:"approved"|"rejected"){const prompt=decision==="approved"?"Banka hesabındaki tahsilatı doğruladınız mı? Onay, ödemeyi ödendi yapıp paketi etkinleştirir.":"Bu havale talebi reddedilsin mi?";if(!window.confirm(prompt))return;setReviewing(row.id);const r=await reviewManualBankTransfer(row.id,decision);setReviewing("");if(r.error)setError(r.error);else void load();}
return <div className="space-y-6"><header className="flex flex-col gap-4 border-b border-border pb-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><CreditCard className="size-6 text-primary"/><h1 className="text-xl font-bold text-ink">Ödemeler / Payments</h1></div><p className="mt-1 text-xs text-muted-foreground">Doğrulanmış işlemleri inceleyin; yalnızca manuel havaleleri açık onayla sonuçlandırın.</p></div><button onClick={load} disabled={loading} className="inline-flex min-h-9 items-center gap-2 rounded-lg border border-border bg-white px-3 text-xs font-semibold"><RefreshCw className="size-3.5"/>Yenile</button></header>{error&&<div role="alert" className="flex items-center gap-2 rounded-xl border border-red-200 bg-red-50 p-4 text-xs text-red-800"><AlertCircle className="size-4"/>{error}</div>}{loading?<div className="rounded-xl border border-border bg-white p-12"><AdminWaveStatus label="Ödemeler yükleniyor…"/></div>:rows.length===0?<div className="rounded-xl border border-dashed border-border bg-white p-12 text-center text-sm text-muted-foreground">Henüz ödeme kaydı yok.</div>:<div className="overflow-hidden rounded-xl border border-border bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[1080px] text-left text-xs"><thead className="border-b border-border bg-background-soft text-[10px] uppercase text-muted-foreground"><tr><th className="px-4 py-3">Öğrenci</th><th className="px-4 py-3">Paket</th><th className="px-4 py-3">Tutar</th><th className="px-4 py-3">Yöntem</th><th className="px-4 py-3">Sağlayıcı</th><th className="px-4 py-3">Durum</th><th className="px-4 py-3">Tarih</th><th className="px-4 py-3">Referans / İşlem</th></tr></thead><tbody className="divide-y divide-border">{rows.map((row)=>{const reviewable=row.payment_method==="bank_transfer"&&row.provider==="manual_bank_transfer"&&["pending","processing","requires_action"].includes(row.status);return <tr key={row.id}><td className="px-4 py-3"><strong className="block">{row.payer_name||"—"}</strong><span className="text-[10px] text-muted-foreground">{row.payer_email||"—"}</span></td><td className="px-4 py-3 font-mono">{row.package_id}</td><td className="px-4 py-3 font-semibold">{money(row.amount,row.currency)}</td><td className="px-4 py-3">{row.payment_method==="bank_transfer"?"Havale / EFT":"Kart"}</td><td className="px-4 py-3">{row.provider}</td><td className="px-4 py-3"><span className="rounded-full border border-border bg-surface-muted px-2 py-1 font-semibold">{labels[row.status]||row.status}</span></td><td className="px-4 py-3 text-muted-foreground">{new Date(row.created_at).toLocaleString("tr-TR")}</td><td className="px-4 py-3"><span className="font-mono text-[10px]">{row.public_reference}</span>{reviewable&&<div className="mt-2 flex gap-1"><button disabled={reviewing===row.id} onClick={()=>void review(row,"approved")} className="rounded bg-ink px-2 py-1 text-white disabled:opacity-50">Tahsilatı Onayla</button><button disabled={reviewing===row.id} onClick={()=>void review(row,"rejected")} className="rounded border border-border px-2 py-1">Reddet</button></div>}</td></tr>})}</tbody></table></div></div>}<p className="text-[11px] leading-5 text-muted-foreground">Kart numarası, CVV veya banka parolası saklanmaz. Kart işlemleri bu panelden manuel olarak ödendi durumuna getirilemez. Her inceleme denetim kaydına yazılır.</p></div>}
function money(amount:number,currency:string){try{return new Intl.NumberFormat("tr-TR",{style:"currency",currency}).format(amount)}catch{return `${amount} ${currency}`}}

/**
 * The former homepage pricing preview is intentionally retired.
 * Public pricing now has one canonical, admin-backed presentation on the
 * localized pricing page.
 */
export function PricingPreview() {
  return null;
}

export default PricingPreview;
